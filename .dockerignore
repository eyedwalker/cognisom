.git
.gitignore
.env
.env.*

# Credentials. These are gitignored, so CI images are clean, but a LOCAL
# `docker build` / `make docker` sees the working tree and would otherwise
# bake them into a layer that gets pushed to ECR.
*.pem
*.key
*.p12
cognisom.txt
secrets/
__pycache__
*.pyc
*.pyo
.pytest_cache
.mypy_cache
data/
exports/
*.egg-info
dist/
build/
.vscode/
.idea/
*.md
Dockerfile*
docker-compose*
deploy/
infrastructure/
.terraform
*.tfstate
*.tfstate.backup
linkedin_articles/
funding/

# Large local artifacts that only bloat the build context.
PhysiCell/
*.mov
*.mp4
tests/
cognisom/tests/
