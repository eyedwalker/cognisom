# GPU-accelerated dependencies for NVIDIA stack
# Install with: pip install -r requirements.gpu.txt

# HTTP client for NIM API calls
requests>=2.31.0

# NVIDIA NeMo Agent Toolkit (orchestration)
# nvidia-nat

# NVIDIA PhysicsNeMo (PDE surrogates) - install when training
# nvidia-physicsnemo

# RAPIDS single-cell analysis - install on GPU instances
# rapids-singlecell

# Scanpy for single-cell workflows
scanpy>=1.9.0
anndata>=0.9.0

# Molecular visualization and processing
# rdkit  # Install via conda: conda install -c conda-forge rdkit

# Environment variable loading
python-dotenv>=1.0.0
